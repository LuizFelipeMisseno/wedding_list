name: wedding_list
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  dartz: ^0.10.1
  equatable: ^2.0.5
  firebase_core: ^2.14.0
  flutter:
    sdk: flutter
  flutter_modular: ^5.0.3
  flutter_triple: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
